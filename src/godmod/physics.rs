#[derive(Debug)]
pub struct Position {
    pub x: f64,
    pub y: f64,
}

#[derive(Debug)]
pub struct Speed {
    pub x: f64,
    pub y: f64,
}

#[derive(Debug)]
pub struct Force {
    pub x: f64,
    pub y: f64,
}

#[derive(Debug)]
pub struct UnitVector {
    pub x: f64,
    pub y: f64,
}
